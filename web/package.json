{
  "name": "cad-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:kernel": "wasm-pack build ../crates/kernel --target web --out-dir ../web/kernel-pkg --out-name kernel",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
