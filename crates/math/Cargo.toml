[package]
name = "math"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Geometric math primitives for the CAD kernel."

[lib]
crate-type = ["rlib"]
